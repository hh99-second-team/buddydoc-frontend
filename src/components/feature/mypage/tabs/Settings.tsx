import React from 'react';
import styled from 'styled-components';

function Settings() {
  return <Layout>해당 서비스는 준비중입니다.</Layout>;
}

export default Settings;

const Layout = styled.div`
  font-size: 40px;
  font-weight: bold;
`;
